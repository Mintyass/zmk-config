# Default TX power (save battery)
# (no TX power override — uses ZMK default)

# Deep sleep after 15 minutes (aggressive — 110mAh battery)
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# Display config
CONFIG_NICE_VIEW_GEM_ANIMATION_MS=3840
